import './PlaceholderTab.css';

export default function DockerTab() {
    return (
        <div className="placeholder-tab">
            <span className="placeholder-tab-title">Docker</span>
            <span className="placeholder-tab-desc">Container config, image, and Dockerfile</span>
        </div>
    );
}
